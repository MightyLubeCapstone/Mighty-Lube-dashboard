Placed json in https://codebeautify.org/jsonviewer which made it much easier to see

- Array is set up to conist of user data

- Each element in first layer of array is a user id

- information for the cart is inside each user id

- user information is stored within second layer of array

- Within the user information, there is a cart and a draft element
    - Draft element contains information currently "in the cart" but not yet ordered
    - Cart information contains items actually ordered

- Cart data includes an orderID, date created, number requested, product information, product type, ect.

- Path to cart data: array -> indexed numbers (users, 0, 1, ect.) -> cart (associated number of carts) -> indexed numbers (particular cart for order, 0, 1, ect.) -> INFORMATION
    - INFORMATION contains orderID, orderCreated (array), numRequested (array), productConfigurationInfo (array), productType, _id